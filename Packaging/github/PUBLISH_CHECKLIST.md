# ShelfBar Public Release Checklist

Tag:

```text
v1.0.0-beta.1
```

Release title:

```text
ShelfBar v1.0 Beta
```

Upload assets:

- `build/Release/ShelfBar.dmg`
- `build/Release/ShelfBar.zip`
- `build/Release/SHA256SUMS.txt`

Before publishing:

- Confirm GitHub repository URL.
- Confirm Developer ID signing / notarization status.
- Confirm `Packaging/homebrew/shelfbar.rb` URL and SHA.
- Enable GitHub Pages from `docs/`.
- Add Gatekeeper screenshot if available.
